 - hosts: localhost
   tasks:
    - name: Shell commands
      shell: |
        pwd
        ls -a1F
      register: shell-result
    - name: Print output
      debug:
        var: shell-result
    - name: Link bee movie
      file:
        src: "../bee movie script.txt"
        dest: ../bms.txt
        state: link
    # - name: Template config
    #   template:
    #     src: site.conf.j2
    #     dest: ../bee.conf