---
 - hosts: localhost
   tasks:
    - name: Nginx role
      include_role:
        name: nginx
      vars:
        subdomain: gwt.lukemoll.com
        # webroot: "{{ toast.slice.path }}"
        proxy_pass: "http://localhost:8000"